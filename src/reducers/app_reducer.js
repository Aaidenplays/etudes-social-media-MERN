import{ getUserSignin, getUserSignup} from '../actions';

defaultState = {
    user:{
      id: undefined,
      name: "",
      email: ""
    }
  }

  export default function(state = defaultState, action) {
      
  }